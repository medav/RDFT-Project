#ifndef __CONSOLE__
#define __CONSOLE__

void SetupConsole();
/*
typedef struct {
	Console::CMDTYPE cty;
	char args[100];
} COMMAND;

class Console {
public:
	enum CMDTYPE {SET, PRINT, NEWMAP};

private:
	void Exec(COMMAND cmd);

public:
	Console();
	
};*/

#endif